// Program to check if a string starts with another string

const string = 'Hello World';

const toCheck = 'He';

if(string.startsWith(toCheckS)) {
    console.log('The string starts with "he".');
}
else {
    console.log('The string does not starts with "he".');
}